/**
 * Haga que la funcion StringScramble(str1, str2) tome los dos parametros que se le pasan
 * y devuelva un booleano
 * true si una parte de los caracteres de str1 se puede reordenar para que coincida con los de str2, y false en caso contrario.
 * por ejemplo: si str1 "wrkqodl" y str2 es "world" la funcion deberia retornar true
 */
"use strict";

function StringScramble(str1, str2) {}

console.log(StringScramble("wrkqodl", "world"));
